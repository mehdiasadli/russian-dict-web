import WordsList from '../components/WordsList';

const ProfileLearning = () => {
  return (
    <div>
      <WordsList isProfile packet='learning' />
    </div>
  );
};

export default ProfileLearning;
